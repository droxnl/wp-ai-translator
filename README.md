# wp-ai-translator
WP Bakery / Salient compatible translation plugin with AI that translate fully page context and makes for every set-upped language a /en/ path (lanuage). Also possible to translate global sections, automatically set-up menu's for different languages.
